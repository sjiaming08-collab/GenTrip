# Docker Compose local runtime
POSTGRES_DB=gentrip
POSTGRES_USER=gentrip
POSTGRES_PASSWORD=gentrip
POSTGRES_PORT=5432
REDIS_PORT=6379
API_PORT=8080

# Keep false for deterministic local smoke tests. Add a real key only to .env.
LLM_ENABLED=false
LLM_API_KEY=
LLM_MODEL=deepseek-v4-pro
