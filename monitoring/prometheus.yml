global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: gentrip-api
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: [api:8080]
